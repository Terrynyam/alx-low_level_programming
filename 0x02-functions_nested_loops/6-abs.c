#include "main.h"

/**
 * _abs - the function computes the absolute value of an integer
 * @n: the in to check
 * Return: int the absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
