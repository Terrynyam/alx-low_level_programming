#include "main.h"

/**
 * add - the function adds two  numbers and return a result
 * @a: a number to be added to b
 * @b: a number to be added to a
 * Return: the sum of a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
