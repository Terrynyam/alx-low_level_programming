#include <stdio.h>

/**
 * main - prints the phrase "with proper grammar,
 * but the outcome is piece of art"
 * Return: 0 if exited properly, otherwise a non-zero value
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
